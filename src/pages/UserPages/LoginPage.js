import React from 'react'
import LoginContainer from '../../containers/UserContainers/LoginContainer';

export default function LoginPage({ history }) {
  return (
    <LoginContainer history={history} />
  )
}
